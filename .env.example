# Torok API keys are optional.

# Free mode (default): local curated wisdom engine — no keys required.
# The app is fully useful with zero environment variables.

# Optional future inference provider (server-side only; never exposed to the browser)
# TOROK_PROVIDER=enabled
# TOROK_API_KEY=
